const { use, useState } = require("react")

const Employeeform = {{ selected Employee ,refresh}} => {
    const [employee, setEmployee]=

    useState({
        name:"",
        email:"",
        department:"",
        position:"",
        salary:"",
    });
}